# `newbuilds` test

In `presentation/` directory, you'll find the presentation explaing my
thinking and approach.

In `clojure/` directory, you'll find my Clojure code.

In `ruby/` directory, you'll find my Ruby code.
